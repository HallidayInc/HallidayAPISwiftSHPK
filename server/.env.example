ALCHEMY_API_KEY=
PORT=3000

# Optional. Both APIs work without a key, but rate limits are low.
TRONGRID_API_KEY=
BLOCKCYPHER_TOKEN=

# Public Halliday key, used only to list which assets exist on RPC-only chains.
HALLIDAY_API_KEY=
